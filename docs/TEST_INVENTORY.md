# Test Inventory

Auto-generated by `npm run docs:test-inventory` (scripts/generate-test-inventory.mjs) — do not hand-edit. Regenerate after adding or renaming any test.

CI re-runs this generator and fails if the committed file doesn't match — see `.github/workflows/ci.yml`.

**43 declared test blocks** across 2 layers as of the last generation. Parametrized blocks (`it.each`) count once here but expand to multiple cases at run time — the real per-run totals (e.g. 37 API / 11 E2E) come from an actual test run, not this count; see the latest report under [reports/](../reports/).

## API / integration — `api/` (Vitest + native `fetch`)

### `api/01-auth.spec.ts` (8 declared blocks)

**auth**
- registers a new user and returns a usable token
- rejects registration with a password under 6 characters (400)
- rejects registering an email that already has an account (400), and does not alter it
- logs in with correct credentials (200 + token)
- REGRESSION (checklist #1): wrong password must return 401 and must NEVER issue a token
- rejects login for an email that was never registered (401, not 500/404)
- GET /auth/me with no token returns 401
- GET /auth/me with a garbage/spoofed bearer token returns 401, not a spoofed identity

### `api/02-isolation.spec.ts` (9 declared blocks)

**unauthenticated access — checklist item #3**
- %s %s returns 401 with no Authorization header _(parametrized — one row per case, see source)_
- a spoofed identity header with no valid token is still rejected

**cross-user isolation — checklist item #4**
- B's schedule GET never includes any of A's slots
- B's habit GET never includes any of A's habit logs
- B's todo GET never includes any of A's todos or notes
- B deleting A's schedule slot by a guessed/known slotKey does not remove it
- B deleting A's todo by known ID does not remove it (data must survive regardless of response code)
- B patching A's todo (toggle complete) by known ID does not change it
- B deleting A's habit log by known ID does not remove it

### `api/03-schedule.spec.ts` (4 declared blocks)

**schedule slots**
- checklist #10 — double-submitting the same slot does not create duplicates (upsert)
- checklist #11 — valid category/status values round-trip exactly
- checklist #11 — an unexpected category string (bypassing the UI dropdown) is stored as-is and does not break a subsequent fetch
- checklist #11 — a category longer than the schema column (50 chars) fails cleanly, not with an unhandled 500

### `api/04-habits.spec.ts` (3 declared blocks)

**habit ledger CRUD (own-user)**
- logs a habit action and it appears in the week view with a running total
- deleting a habit log removes exactly that entry, nothing else
- a custom entry with a negative point value (deduction) round-trips correctly

### `api/05-todos.spec.ts` (3 declared blocks)

**todo & notes persistence round-trip**
- checklist #5 — full add/toggle/delete round trip leaves exactly the expected state
- weekly scratchpad notes save and are returned verbatim on the next fetch
- toggling completion back to false is respected (not a one-way flag)

### `api/06-restart-persistence.spec.ts` (1 declared block)

**persistence survives a real database restart**
- checklist #6 — a note and a todo written before restart are still there after

### `api/07-proxy.spec.ts` (4 declared blocks)

**rate limiting through the real reverse-proxy path**
- checklist #7 — a client tripping the limit gets 429 (confirmed MAX_ATTEMPTS=50/15min for non-localhost IPs, from rateLimiter.ts)
- checklist #7b — a client can't dodge the limit by forging its own X-Forwarded-For (RESOLVED, was previously red — server.ts now sets app.set('trust proxy', 1). This test's original form tried to simulate "two different clients" purely from this one test-runner machine with no distinguishing signal at all, which could never pass regardless of the fix — trust proxy=1 correctly trusts only what nginx itself directly observed, one hop back, and correctly ignores a value the client injects further back in the chain, which is what that attempt actually was. This rewrite tests something trust proxy=1 can really prove: spoofing X-Forwarded-For must NOT be a way to evade the limiter.)

**CORS**
- checklist #8 — a disallowed Origin does not get reflected in Access-Control-Allow-Origin
- the QA web origin (configured via ALLOWED_ORIGINS) IS allowed

## Web — `e2e/` (Playwright)

### `e2e/login-gate.spec.ts` (4 declared blocks)

**login gate**
- an unauthenticated visitor sees the login screen, not the app shell
- registering with a fresh email lands on the authenticated grid
- logging in with the wrong password stays on the login screen with an error, never enters the app
- logout returns to the login screen

### `e2e/time-lock.spec.ts` (2 declared blocks)

**Planned Task time-lock**
- a slot from a past day: Planned Task is locked, Actual Task remains editable
- a slot from a future day: Planned Task is fully editable, no lock message

### `e2e/todo-persistence.spec.ts` (2 declared blocks)

**todo UI state survives a real reload**
- an added todo, its completed state, and a deleted sibling all survive reload
- weekly scratchpad notes survive reload

### `e2e/view-modes.spec.ts` (3 declared blocks)

**schedule grid view modes**
- switching Day / Week / Month updates the active button and grid content
- the current 30-minute slot is highlighted as "NOW" in Week view
- the category filter narrows the grid to a single category
